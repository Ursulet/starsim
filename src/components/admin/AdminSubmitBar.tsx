export function AdminSubmitBar({ children }: { children: React.ReactNode }) {
  return <div className="sticky bottom-0 mt-6 rounded-2xl border border-slate-200 bg-white/95 p-4 shadow-soft backdrop-blur">{children}</div>;
}
